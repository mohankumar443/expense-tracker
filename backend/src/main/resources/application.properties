spring.application.name=expensetracker

# H2 Database (for existing expense tracker)
# spring.datasource.url=jdbc:h2:mem:expensedb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true

# MongoDB Configuration (for debt tracker)
# MongoDB connection comes from env SPRING_DATA_MONGODB_URI. Fallback (local) can be set here if needed:
# spring.data.mongodb.uri=mongodb://localhost:27017/debt-tracker
spring.data.mongodb.database=debt-tracker
spring.data.mongodb.auto-index-creation=true
